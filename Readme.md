# JSON Server Quick Start Guide

## Installation

To install JSON Server globally, open your terminal or command prompt and run the following command:

```bash
npm install -g json-server
```

## Run json server

```bash
json-server --watch src/db/db.json --port 3000
```